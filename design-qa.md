# Key 额度工作台设计验收

## 对照对象

- 设计图：`C:\Users\10491\.codex\generated_images\019fcab4-9b02-7dd2-9181-379bc9f6cd47\exec-f9d96507-0ab5-4b14-a418-6187aac359f1.png`（1487 × 1058）。
- 实现截图：`C:\Users\10491\AppData\Local\Temp\codex-carpool-key-workbench-v26.png`（1265 × 712，CSS 视口 1265 × 712，DPR 1）。
- 比较状态：浅色主题、已打开“开发组 Key”详情、近 7 天、按日统计。
- 归一化：以居中的 920 × 640 Key 工作台为主进行对照；设计图的背景 Key 列表为概念示意，生产页面保留现有的统计与日志背景。

## 对照证据

- 已在同一次视觉对照中打开设计图和浏览器实拍图。
- 全视图：两者都采用“紧凑 Key 列表 + 居中、不裁切的详情工作台”；标题、筛选、额度、本期概览、Token 结构、趋势及页脚操作顺序一致。
- 重点区域：工作台的额度区为两行真实美元额度进度；本期概览为 2 × 2 指标；Token 三列与趋势图均在无需内部滚动时完整可见。

## 验收结果

**Findings**

- 未发现 P0/P1/P2 视觉问题。
- 有意偏差：设计图中的“区间消耗（USD）”与“最后请求”没有对应的分析接口字段；实现改为真实可得的“周期 Token / 累计 Token”，避免展示伪造数值。
- 有意偏差：趋势继续使用现有 API 返回的已完成 Token 柱状数据，不伪造按美元的面积图。

**Required Fidelity Surfaces**

- 字体与层级：沿用面板的 Inter / PingFang SC 字体链；Key 名称、区块标题、数值和辅助文字层级清晰，没有截断。
- 间距与布局：对话框固定为 920 × 640；在当前 1265 × 712 视口内，正文 `scrollHeight === clientHeight`，没有内部滚动条或被页脚遮挡的指标。
- 颜色与令牌：详情工作台只使用 `--cc-*` 主题变量及限量的绿色语义色；顶层 dialog 样式均以 `#key-detail-drawer` 为根，避免 CPA 宿主选择器覆盖。
- 图像与图标：没有新增图像或手绘图标；沿用现有图标精灵。关闭、查询、重置、解除管理、编辑策略图标均完整显示。
- 文案与数据：所有显示值来自当前 Key 的预算、美元消耗与 CPA 回传 Token；三类 Token 都为 0 时保留空轨道，不再误显示为三等分比例。

**交互验证**

- 点击 Key：打开详情工作台，显示 2 个额度行、4 个本期指标、3 个 Token 指标及趋势柱。
- 点击关闭：dialog 正常关闭；再次点击 Key：正常重新打开。
- 切换为“近 7 天”：起止日期更新为 2026-08-04 至 2026-08-10，统计粒度切换为按日。
- 浏览器控制台：无日志错误。

**Theme And Host Coverage**

- 深色主题的变量与根选择器已静态核对：`html[data-cpa-theme="dark"] .cc-panel` 会覆盖 `--cc-*` 变量，顶层工作台不依赖全局裸选择器。
- 预览浏览器的 DOM 适配层不允许切换根主题属性，因此实际 CPA 宿主的浅色 / 深色主题仍需在部署后各打开一次确认；这不影响本次预览页的通过结论。

**Implementation Checklist**

- Key 列表保持紧凑，选择态只用窄绿色左边线和浅色背景。
- 详情使用静态弹窗骨架；操作按钮不在运行时追加。
- 额度、本期、Token、趋势和操作分区固定，所有关键指标可见。

final result: passed
