# codex-carpool 部署与使用说明

`codex-carpool` 是 CPA 的全模型 Key 美元计量插件。CPA 继续负责认证文件和调度，插件只保存自己的 Key 策略、模型费率、美元账本和审计日志。

## 最终规则

1. 没有启用策略的 Key 不限制，直接走 CPA 原调度，也不会写入插件用量。
2. CPA 每次调度把候选 AuthID 传给插件。插件不筛选账号池，只把选中的候选 ID 原样交回 CPA。
3. 每个受管 Key 分别配置 5 小时和 7 天美元预算；留空或 `0` 表示不限额。
4. 模型目录从 CPA 同步。费率设置按模型填写输入、缓存输入、输出三项 USD / 百万 Token；未配置费率的模型返回 `503`，三项都为 `0` 的模型免费。
5. CPA 请求完成后回传实际 Token。插件按“输入（扣除缓存部分）× 输入费率 + 缓存 Token × 缓存费率 + 输出 Token × 输出费率”计算美元，同时写入两个窗口和使用日志。未回传实际 Token 时按未完成请求记录，Token 和美元都记为 `0`，不使用固定值估算。
6. 外部 Key 的 CPA 回调识别为非受管后直接忽略，不会归集到内部 Key。
7. 违禁词、访问时段、用量分析和日志只作用于受管 Key；运行日志会记录启动、模型同步、费率保存、结算和错误。

## 文件位置

```text
dist/codex-carpool_0.5.20.so
/CLIProxyAPI/plugins/linux/amd64/codex-carpool_0.5.20.so
/CLIProxyAPI/plugins/codex-carpool/data/codex-carpool.db
```

数据目录必须可写，且只能挂给一个 CPA 实例。插件不会写入 CPA 认证文件或原始 API Key。

当前版本只接受新的美元计费数据库，不读取或迁移此前的计量库；首次部署请使用空数据库。插件安全重载时会保存未完成请求的回调标记，重载后继续按原请求时间和原费率结算。

## 编译与安装

在 Linux amd64 构建机准备 Go 1.26+、CGO C 编译器、`make` 与 `zip`：

```bash
chmod +x build-linux.sh
VERSION=0.5.20 ./build-linux.sh
```

构建脚本会运行单元测试、竞态测试和 `go vet`，成功后生成共享库和 ZIP 包。

```bash
install -D -m 0755 \
  dist/codex-carpool_0.5.20.so \
  /CLIProxyAPI/plugins/linux/amd64/codex-carpool_0.5.20.so

mkdir -p /CLIProxyAPI/plugins/codex-carpool/data
chmod 700 /CLIProxyAPI/plugins/codex-carpool/data
```

CPA 配置只需要负责加载插件：

```yaml
plugins:
  enabled: true
  dir: /CLIProxyAPI/plugins
  configs:
    codex-carpool:
      enabled: true
      priority: 100
```

重启 CPA 前删除插件目录中的旧版同名共享库，确保只加载一个版本；数据目录保持可写。

## 面板操作

1. 在 CPAMP 打开 `/v0/resource/plugins/codex-carpool/panel`。
2. 点击“同步 CPA Key 与模型”，获取当前 CPA Key 和全部支持的模型。
3. 打开“费率设置”，为模型填写输入、缓存、输出价格。首次空费率卡会自动写入内置种子，之后不会覆盖人工配置。
4. 新增 Key，选择 CPA Key，填写备注和 5 小时 / 7 天美元预算；留空或 `0` 为不限额。
5. 在 Key 详情查看窗口消耗和 Token 汇总，在“使用日志”查看输入、缓存、输出、美元消耗和 CPA AuthID，在“运行日志”查看插件状态。

## 内置费率种子

首次空配置会加入：

```text
gpt-5.3-codex-spark
gpt-5.4-mini
gpt-5.6-sol
gpt-5.6-luna
gpt-image-1.5
gpt-image-2 (input/cache/output = 0)
```

## 管理接口

| 方法 | 路由 | 用途 |
| --- | --- | --- |
| GET / PUT | `/v0/management/codex-carpool/setup` | 插件运行设置 |
| GET | `/v0/management/codex-carpool/summary` | Key 美元窗口和 Token 状态 |
| GET / POST / PUT / DELETE | `/v0/management/codex-carpool/keys` | Key 策略管理 |
| POST | `/v0/management/codex-carpool/keys/reset?key_id=...` | 重置单 Key 美元用量，保留日志 |
| GET | `/v0/management/codex-carpool/analysis?key_id=...` | 单 Key Token 分析 |
| GET / DELETE | `/v0/management/codex-carpool/logs?key_id=...` | 使用日志 |
| GET / DELETE | `/v0/management/codex-carpool/operation-logs` | 运行日志 |
| GET / PUT | `/v0/management/codex-carpool/content-filter` | 违禁词设置 |
| GET / DELETE | `/v0/management/codex-carpool/forbidden-logs` | 违禁词日志 |
| GET / PUT | `/v0/management/codex-carpool/models` | CPA 模型目录 |
| GET / PUT | `/v0/management/codex-carpool/rates` | 模型费率 |

## 上线前检查

- 未管理 Key 仍走 CPA 原调度。
- 未配置费率的模型返回 `503`；三项费率为 `0` 时不扣费。
- 任一美元窗口达到上限返回 `429`，窗口冷却后恢复。
- 终端回调后，日志和美元窗口都能看到输入、缓存、输出 Token 与费用。
- 外部 Key 用量不出现在受管 Key 统计中。
- 重启 CPA 后费率、策略、账本和日志仍存在。
