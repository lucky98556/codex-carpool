# codex-carpool 部署与使用说明

`codex-carpool` 是 CPA 的 Codex 拼车插件。它不改 CPA 左侧菜单、认证文件、原 API Key 或全局调度配置；只在插件自己的 SQLite 中维护共享账号池、Key 分配、模型限制、官方额度快照和用量审计。

## 最终规则

1. **没有配置策略的 Key 不限制。** 它直接回到 CPA 原调度，插件不计量也不拦截。
2. 先在“配置账号池”中添加 CPA 已有 Codex 账号，并填写账号容量，例如 `1x`、`20x`。容量是管理员定义的可分配份额，不是官方接口返回的百分比。
3. 每个受控 Key 只有一个 `x` 分配、一个备注和一个模型白名单；所有启用 Key 的分配总和不能超过启用账号池容量。它不再分别填写 5 小时和 7 天倍率，分组、百分比和并发字段也不会被新 API 接受。
4. 不选择模型即默认允许全部已从 CPA 自动同步的 Codex 模型；选择白名单外模型会在上游请求前返回 `403`。
5. 官方 5 小时和周窗口是账号池的最终硬额度。插件为每个账号直接读取官方 usage 快照，按“容量 × 官方剩余比例”选择当前最有余量的可用账号；同时每个受控 Key 的唯一 `x` 分配会形成一个近 7 天 Token 守卫，预留请求并按完成 Token 结算，避免一个 Key 在快照轮询间隔内耗尽整个共享池。一个 `1x` 账号耗尽时，会自动跳过并切换到可用的 `20x` 账号。
6. 官方请求以插件自身 HTTP 客户端直连 `chatgpt.com`，只通过 CPA 宿主读取当前认证凭据；不进入 CPA 的模型代理、调度、通用请求记录或下游用量链路。凭据不写入 SQLite、不返回浏览器，直连请求最长 15 秒。如服务器必须出网代理，请给 CPA 进程设置标准 `HTTPS_PROXY` / `HTTP_PROXY` 环境变量；插件不会复用 CPA 的模型代理设置。
7. 启动、账号池变更、快照过期、每 5 分钟及上游 `429` 后都会排队刷新；调度请求永不等待官方网络调用。只有 `usage_limit_reached` 会立即暂停账号，其他临时 `429` 仅刷新快照；无可用快照时返回 `503`，所有配置账号耗尽或 Key 用尽分配时返回 `429`。
8. 单 Key 的 Token 趋势、完成记录和策略日志为插件独立数据，不保存提示词、请求正文、明文 API Key 或 OAuth token。面板底部还会显示独立的“插件运行与错误日志”，记录启动、配置变更、模型同步、官方额度同步失败/恢复及账号耗尽；相同账号的重复同步错误最多每 5 分钟记录一次。

> `x` 同时是共享池承诺与单 Key 的运行时分配比例：它先防止管理员把 `20x` 的账号池配置成 `30x` 的 Key 承诺，再以完成 Token 做近 7 天保守守卫；官方账号窗口仍是实际可消耗总量的最终边界。

## 文件位置

```text
dist/codex-carpool.so
/CLIProxyAPI/plugins/linux/amd64/codex-carpool.so
/CLIProxyAPI/plugins/codex-carpool/data/codex-carpool.db
```

数据目录必须可写，且只能挂给一个 CPA 实例。它包含账号池配置、官方快照、Key 的 HMAC 指纹、分钟级 Token 聚合、模型目录及审计日志；不写 CPA 认证文件或 API Key 配置。

## 编译与安装

在 Linux amd64 构建机准备 Go 1.26+、C 编译器、`make` 与 `zip`：

```bash
chmod +x build-linux.sh
./build-linux.sh
```

安装产物并准备可写数据目录：

```bash
install -D -m 0755 \
  dist/codex-carpool.so \
  /opt/1panel/www/sites/cliproxyapi/plugins/linux/amd64/codex-carpool.so

mkdir -p /opt/1panel/www/sites/cliproxyapi/plugins/codex-carpool/data
chmod 700 /opt/1panel/www/sites/cliproxyapi/plugins/codex-carpool/data
```

若插件目录整体只读，额外挂载插件数据子目录：

```yaml
volumes:
  - /opt/1panel/www/sites/cliproxyapi/plugins:/CLIProxyAPI/plugins:ro
  - /opt/1panel/www/sites/cliproxyapi/plugins/codex-carpool:/CLIProxyAPI/plugins/codex-carpool:rw
```

`config.yaml` 只需要负责加载插件：

```yaml
plugins:
  enabled: true
  dir: /CLIProxyAPI/plugins
  configs:
    codex-carpool:
      enabled: true
      priority: 100
```

## 面板操作

1. 在 CPAMP 完成管理登录后，从 CPA 左侧插件入口打开 `codex-carpool`；页面只显示右侧内容并跟随 CPAMP 主题。
2. 点击“配置账号池”，从插件自动发现的 CPA Codex 账号中选择账号，填写容量 `x` 后保存。保存会立即排队直连官方 usage；右侧账号池显示 5 小时、周剩余比例与最近恢复时间。已有受控请求处于官方周账期时，不能增删账号或修改账号容量、启停状态，需等这些账期官方刷新后再改。
3. 点击“纳入 Key 管理”，从 CPA 已有 Key 中选择一个，填写备注和唯一的 `x` 分配。若总分配超出启用账号池容量，保存会被拒绝。
4. 不勾选模型即全部允许；勾选后仅允许选中的 CPA 同步模型。原始 Key 仅在浏览器当前对话框中短暂存在，保存后插件只保留 HMAC 指纹。
5. Key 详情会显示近 7 天分配用量、趋势与日志；页面底部的“插件运行与错误日志”可按全部、错误、警告或运行事件筛选。分配守卫用完成 Token 计算并在达到该 Key 的 x 上限时返回 `429`；可以随时停用 Key，但其未过官方周账期的 x 仍占用共享池。解除 Key 管理、移除账号、调整账号容量/启停或修改已活跃 Key 的 x，必须等待相关官方周账期刷新，以免中途产生第二份额度。

## 迁移与上线检查

旧 `codex-quota-guard` 数据库首次启动时可复制到新目录，旧文件不删除。旧百分比/分组策略会暂停并标记“需重新绑定”，不会阻塞插件启动，也不会误限流。

上线前至少验证：

1. 未纳入管理的 Key 正常走 CPA。
2. 受控 Key 的模型白名单外请求返回 `403`。
3. 添加 `1x` 与 `20x` 两个账号后，官方快照显示在右侧账号池中。
4. 将 `1x` 账号置为官方耗尽或返回 `usage_limit_reached` 后，新请求会选择可用 `20x` 账号；全池耗尽才返回 `429`。普通临时 `429` 不会冻结整个账号。
5. 断网或官方接口异常时，快照在 10 分钟后不再被选用；无可用快照返回 `503`，不会假装额度正常。
6. 重启 CPA 后，账号池、Key 分配、模型策略、近期 Token 趋势和日志仍存在。
